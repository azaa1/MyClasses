// print an interger 'count'
#include<stdio.h>
int main()
{
   // assigning a value (23) to interger 'count'
int count = 23;
   // printf() displays the string inside quotation
   printf("%d", count);
}
